# No AI keys: the label reader runs on the device, at no cost
# (docs/label-recognition). A VITE_* variable ends up in the browser bundle.

# App URL (for QR code generation)
VITE_APP_URL=https://pickd.pages.dev
