# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.pnpm-store
dist
dist-ssr
*.local
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
tmp/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Designs/Assets
disenios/

# Backups
backups/

# Test & Dev Artifacts
playwright-report/
playwright/.auth/
test-results/
supabase/.temp/
tsc_output_expert.txt
test_db.*
.agent/skills/
.agents/skills/
claude-skills/

# Claude skills live IN the repo (vendored 11 Aug 2026). They used to be symlinks
# into a central skills repo, which broke the day that repo moved on disk. Keeping
# real copies here makes a clone self-sufficient — see docs/claude-agents-and-skills.md

# Claude local worktrees + per-user tooling config
.claude/worktrees/
.claude/launch.json

# Local Dev Data
supabase/seed.sql
supabase/snippets/
pickd.db
public/reports/daily/

# Performance reports
reports/performance/*.txt

scripts/.sync-progress
.env.bak-*

# Per-machine Claude Code permissions/hooks — paths and container names are
# local to one checkout, so this file must not travel with the repo.
.claude/settings.local.json
