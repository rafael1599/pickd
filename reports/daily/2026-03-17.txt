Progress Update — March 17, 2026


Summary:

Investigated and resolved picking screen display issues, improved pick instructions for warehouse team, and continued 3D number printing.


Still Pending:

- Update the database with the latest changes so everything stays in sync and working correctly.
- Set up the Bay 2 computer with the latest improvements so that when multiple orders come in from the same shop, their PDFs get combined automatically into one. That way the team won't have to search for each order separately — everything from the same shop will be together in one place, ready to pick.


--- Details below ---


Double Check Screen Fixes:

- Investigated why product names were not showing on the verification screen for some orders. After a thorough audit, we confirmed the system is working correctly — the issue only affected older orders that were imported before a recent update. All new orders already include product names from our database.
- As a safety net, the screen now always shows the product name: it pulls from our database first, and if that's empty (for older imports), it uses the name from the PDF. This means no order will ever show up without a product name again.
- The pick instructions below each item now show useful information. Before, it was just repeating the quantity to pick (which was already visible). Now it tells the picker where to grab items from — for example, "Pick from LINE with 4 units" or "Pick from TOWER with 30 units." This helps the team know exactly which storage unit to go to.
- Long-pressing an item now opens the edit form directly instead of showing an intermediate detail screen first. One less tap to make quick corrections.


Testing Needed:

- These changes still need to be tested in production. To test: drop a PDF order into the computer running watchdog-pickd in Bay 2 the same way you normally would, then open the order on the verification screen and confirm that (1) product names show up for every item, (2) the pick-from instructions make sense and match the actual storage layout, and (3) long-pressing an item opens the edit form directly.


3D Printing — Location Numbers:

- Finished designing the full set of 3D numbers for warehouse locations.
- Numbers 0 through 7 are fully printed. Number 8 was left printing at the end of the day.


Developer Tools:

- Set up shared command shortcuts for the project so that common operations (database queries, daily reports, commit messages) are available to any developer working on the project, not just stored locally.


Coming Up:

- Finish printing number 8 and print number 9.
- Test the picking screen changes in production through Bay 2.
- Look into combining multiple orders from the same shop into a single picking session.
- Set up the PDF import tool to start automatically when the computer turns on.


Backlog Status:

0 completed today — 11 pending — 0 new items added today
